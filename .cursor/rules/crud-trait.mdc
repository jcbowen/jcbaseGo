# CRUD Trait 组件使用规范

## 组件概述

CRUD Trait 组件位于 [component/trait/crud/](mdc:component/trait/crud/)，提供了完整的增删改查功能，支持丰富的钩子方法进行业务逻辑扩展。

### 主要特性

- 🚀 **开箱即用**: 提供完整的 CRUD 操作
- 🔧 **高度可扩展**: 丰富的钩子方法支持自定义逻辑
- 🛡️ **事务安全**: 自动事务管理，支持回滚
- 📊 **分页支持**: 内置分页功能
- 🗑️ **软删除**: 支持软删除和硬删除，可自定义软删除字段名
- 🔍 **灵活查询**: 支持复杂查询条件
- 🔄 **批量操作**: 支持批量删除和更新
- 📝 **字段设置**: 支持单个字段值设置

## 核心架构

### Trait 结构体
```go
type Trait struct {
    // ----- 基础配置 ----- //
    PkId               string       `default:"id"` // 数据表主键
    Model              any          // 模型指针
    ModelTableAlias    string       // 模型表别名
    DBI                orm.Instance // 数据库实例
    ListResultStruct   interface{}  // 列表返回结构体
    DetailResultStruct interface{}  // 详情返回结构体
    Controller         interface{}  // 控制器

    // ----- 初始化时生成 ----- //
    ModelTableName      string   // 模型表名
    ModelFields         []string // 模型所有字段
    SoftDeleteField     string   // 软删除字段名
    SoftDeleteCondition string   // 软删除判断条件
    OperateTime         string   // 操作时间
    TableAlias          string   // 表别名
}
```

### Instance 接口
```go
// 统一数据库实例接口，支持调试日志注入
type Instance interface {
    GetDb() *gorm.DB                             // 获取数据库连接
    GetConf() interface{}                        // 获取配置信息（MySQL/SQLite）
    SetDebuggerLogger(debugger.LoggerInterface)  // 注入调试日志记录器
    GetDebuggerLogger() debugger.LoggerInterface // 获取调试日志记录器
}
```

### 控制器接口
```go
// 控制器需实现以下接口以供 Trait 调用
type ControllerInterface interface {
    CheckInit(ctx any) *Context                     // 初始化上下文与参数解析
    GetLogger() debugger.LoggerInterface            // 提供调试日志记录器
}
```

## 基础使用方法

### 控制器集成
```go
package user

import (
    "github.com/jcbowen/jcbaseGo/component/trait/crud"
    "github.com/jcbowen/jcbaseGo/component/orm/mysql"
    "officeAutomation/controllers/base"
    "officeAutomation/library"
    userModel "officeAutomation/model/common/user"
)

type Index struct {
    base.Controller
    *crud.Trait
}

// New 初始化 CRUD 控制器
func New() *Index {
    // MySQL 示例
    index := &Index{
        Trait: &crud.Trait{
            Model: &userModel.Account{},  // 数据模型
            DBI:   library.Mysql,        // 数据库实例
        },
    }
    
    index.Trait.Controller = index
    // 注入调试日志记录器，便于在 CRUD 内部输出统一日志
    index.Trait.DBI.SetDebuggerLogger(index.GetLogger())
    return index
}

// SQLite 示例
/*
func New() *Index {
    sqliteDb, err := sqlite.New(jcbaseGo.SqlLiteStruct{
        DbFile: "path/to/your/database.db",
    })
    if err != nil {
        panic(err)
    }
    
    index := &Index{
        Trait: &crud.Trait{
            Model: &userModel.Account{},
            DBI:   sqliteDb,
        },
    }
    
    index.Trait.Controller = index
    return index
}
*/
```

### 路由配置
```go
systemGroup := r.Group("/system")
systemGroup.Use(middleware.LoginRequired())
{
    systemUserGroup := systemGroup.Group("/user")
    {
        systemUser := systemUserController.New()
        systemUserGroup.GET("/list", systemUser.ActionList)        // 获取列表
        systemUserGroup.GET("/detail", systemUser.ActionDetail)    // 获取详情
        systemUserGroup.POST("/create", systemUser.ActionCreate)   // 创建
        systemUserGroup.POST("/update", systemUser.ActionUpdate)   // 更新
        systemUserGroup.POST("/save", systemUser.ActionSave)       // 保存（自动判断创建/更新）
        systemUserGroup.POST("/delete", systemUser.ActionDelete)   // 删除
        systemUserGroup.GET("/all", systemUser.ActionAll)          // 获取所有数据
        systemUserGroup.POST("/set-value", systemUser.ActionSetValue) // 设置字段值
    }
}
```

## API 接口详解

### ActionCreate - 创建数据
- **HTTP方法**: POST
- **功能**: 创建新的数据记录
- **请求参数**: 模型对应的字段数据（JSON格式）
- **返回**: 创建成功的数据记录

**请求示例**:
```json
{
    "username": "john_doe",
    "email": "john@example.com",
    "status": 1
}
```

### ActionUpdate - 更新数据
- **HTTP方法**: POST
- **功能**: 更新现有数据记录
- **请求参数**: 包含主键ID和要更新的字段数据
- **字段过滤**: 标记为 `gorm:"-:update"` 或 `gorm:"-:all"` 的字段不会被更新
- **返回**: 更新成功的数据记录

**请求示例**:
```json
{
    "id": 123,
    "username": "john_updated",
    "email": "john_new@example.com"
}
```

### ActionDelete - 删除数据
- **HTTP方法**: POST
- **功能**: 删除数据记录（支持批量删除）
- **请求参数**: `{主键名}s` - ID数组
- **返回**: 删除成功的消息

**请求示例**:
```json
{
    "ids": [1, 2, 3]
}
```

### ActionList - 获取数据列表
- **HTTP方法**: GET
- **功能**: 获取分页数据列表
- **查询参数**:
  - `page`: 页码（默认1）
  - `page_size`: 每页数量（默认10，最大1000）
  - `show_deleted`: 是否显示已删除数据（0/1，默认0）
- **返回**: 包含分页信息的数据列表

**请求示例**: `GET /api/user/list?page=1&page_size=20`

### ActionDetail - 获取数据详情
- **HTTP方法**: GET
- **功能**: 获取单条数据的详细信息
- **查询参数**: 主键ID
- **返回**: 数据详情

**请求示例**: `GET /api/user/detail?id=123`

### ActionAll - 获取所有数据
- **HTTP方法**: GET
- **功能**: 获取所有数据（不分页）
- **查询参数**:
  - `show_deleted`: 是否显示已删除数据（0/1，默认0）
- **返回**: 所有数据列表

**请求示例**: `GET /api/user/all`

### ActionSave - 智能保存
- **HTTP方法**: POST
- **功能**: 自动判断创建或更新操作
- **逻辑**: 如果包含主键ID则更新，否则创建
- **请求参数**: 模型对应的字段数据
- **返回**: 保存成功的数据记录

### ActionSetValue - 设置字段值
- **HTTP方法**: POST
- **功能**: 设置单个字段的值
- **请求参数**:
  - 主键ID
  - `field`: 字段名
  - `value`: 字段值
- **字段过滤**: 标记为 `gorm:"-:update"` 或 `gorm:"-:all"` 的字段不允许设置
- **返回**: 设置成功的消息

**请求示例**:
```json
{
    "id": 123,
    "field": "status",
    "value": 0
}
```

## 软删除配置详解

### 配置方式
通过在模型字段的 `gorm` 标签中添加 `soft_delete` 标签来配置软删除：

```go
import "github.com/jcbowen/jcbaseGo/component/orm/base"

type User struct {
    base.MysqlBaseModel  // 包含默认的软删除字段
    Name string `gorm:"column:name;size:100" json:"name"`

    // 方式1: 使用系统默认的 deleted_at 字段（推荐）
    // 基础模型已包含，无需额外定义

    // 方式2: 自定义软删除字段，条件为 IS NULL
    // IsDeleted string `gorm:"column:is_deleted;type:DATETIME;soft_delete:IS NULL" json:"is_deleted"`

    // 方式3: 使用状态字段作为软删除，条件为 = 1（表示正常状态）
    // Status int `gorm:"column:status;type:INT;soft_delete:= 1" json:"status"`

    // 方式4: 使用特殊默认值的 deleted_at 字段
    // DeletedAt string `gorm:"column:deleted_at;type:DATETIME;default:0000-00-00 00:00:00" json:"deleted_at"`
}
```

### 配置说明
1. **默认配置**：如果模型中存在 `deleted_at` 字段且没有 `soft_delete` 标签，系统会自动将其作为软删除字段，条件为 `IS NULL`
2. **自定义字段名**：通过 `soft_delete` 标签可以指定任意字段作为软删除字段
3. **自定义条件**：`soft_delete` 标签的值就是软删除的判断条件
4. **特殊默认值**：如果 `deleted_at` 字段的默认值为 `0000-00-00 00:00:00`，系统会自动使用相应条件

### 使用效果
- **查询时**：系统会自动添加软删除条件，只查询未删除的数据
- **删除时**：执行软删除操作，更新软删除字段的值
- **显示已删除数据**：通过 `show_deleted=1` 参数可以查看已删除的数据

## 钩子方法系统

### 执行时机图
```
创建流程:
FormData → CreateBefore → [事务开始] → 插入数据 → CreateAfter → [事务提交] → CreateReturn

更新流程:
FormData → 查询原始数据 → UpdateBefore → [事务开始] → 更新数据 → UpdateAfter → [事务提交] → UpdateReturn

删除流程:
获取参数 → 查询要删除的数据 → DeleteBefore → [事务开始] → 删除数据 → DeleteAfter → [事务提交] → DeleteReturn

保存流程:
判断是否有主键ID → 调用Create或Update流程
```

### 初始化流程说明
- `InitCrud(ginContext, actionName)` 统一解析请求上下文，准备 GPC 参数、模型信息、软删除条件、表别名等。
- 当控制器实现了同名自定义方法（如 `CreateBefore` 等），Trait 会优先调用控制器的方法；否则回退到默认实现。

### 创建操作钩子

#### CreateBefore - 创建前钩子
```go
// 创建前的数据验证和预处理
func (i *Index) CreateBefore(modelValue interface{}, mapData map[string]any) (interface{}, map[string]any, error) {
    user := modelValue.(*userModel.Account)

    // 验证用户名是否已存在
    var count int64
    i.DBI.GetDb().Model(&userModel.Account{}).Where("username = ?", user.Username).Count(&count)
    if count > 0 {
        return nil, nil, errors.New("用户名已存在")
    }

    // 密码加密
    if user.Password != "" {
        hashedPassword, _ := bcrypt.GenerateFromPassword([]byte(user.Password), bcrypt.DefaultCost)
        user.Password = string(hashedPassword)
    }

    // 设置默认状态
    if user.Status == 0 {
        user.Status = 1
    }

    return user, mapData, nil
}
```

#### CreateAfter - 创建后钩子
```go
// 创建后的后续处理（在事务内执行）
func (i *Index) CreateAfter(tx *gorm.DB, modelValue interface{}) error {
    user := modelValue.(*userModel.Account)

    // 创建用户资料
    profile := &userModel.Profile{
        UserID: user.ID,
        Avatar: "default_avatar.png",
    }
    if err := tx.Create(profile).Error; err != nil {
        return err // 返回错误会回滚事务
    }

    // 记录操作日志
    log.Printf("用户创建成功: ID=%d, Username=%s", user.ID, user.Username)

    return nil
}
```

#### CreateReturn - 创建返回钩子
```go
// 自定义创建成功后的返回格式
func (i *Index) CreateReturn(item any) bool {
    user := item.(*userModel.Account)
    // 隐藏敏感信息
    user.Password = ""
    
    // 自定义返回格式
    response := map[string]interface{}{
        "message": "用户创建成功",
        "data":    user,
    }
    
    i.Result(200, "success", response)
    return true
}
```

### 更新操作钩子

#### UpdateBefore - 更新前钩子 ⭐ 优化版
```go
// 更新前的数据验证（可以访问原始数据）
func (i *Index) UpdateBefore(modelValue interface{}, mapData map[string]any, originalData interface{}) (interface{}, map[string]any, error) {
    user := modelValue.(*userModel.Account)
    original := originalData.(*userModel.Account)

    // 如果用户名有变化，检查是否已存在
    if user.Username != original.Username {
        var count int64
        i.DBI.GetDb().Model(&userModel.Account{}).
            Where("username = ? AND id != ?", user.Username, original.ID).
            Count(&count)
        if count > 0 {
            return nil, nil, errors.New("用户名已存在")
        }
    }

    // 如果密码有变化，进行加密
    if user.Password != "" && user.Password != original.Password {
        hashedPassword, _ := bcrypt.GenerateFromPassword([]byte(user.Password), bcrypt.DefaultCost)
        user.Password = string(hashedPassword)
    } else {
        // 保持原密码
        user.Password = original.Password
    }

    return user, mapData, nil
}
```

### 删除操作钩子

#### DeleteBefore - 删除前钩子
```go
// 删除前的验证和预处理
func (i *Index) DeleteBefore(delArr []map[string]interface{}, delIds []interface{}) ([]interface{}, error) {
    // 检查是否有关联数据
    for _, item := range delArr {
        userID := item["id"]
        
        // 检查是否有关联的订单
        var orderCount int64
        i.DBI.GetDb().Model(&orderModel.Order{}).Where("user_id = ?", userID).Count(&orderCount)
        if orderCount > 0 {
            return nil, fmt.Errorf("用户ID %v 还有关联的订单，无法删除", userID)
        }
    }

    return delIds, nil
}
```

#### DeleteAfter - 删除后钩子
```go
// 删除后的清理工作（在事务内执行）
func (i *Index) DeleteAfter(tx *gorm.DB, delIds []interface{}, delArr []map[string]interface{}) error {
    // 删除相关的用户资料
    if err := tx.Where("user_id IN ?", delIds).Delete(&userModel.Profile{}).Error; err != nil {
        return err
    }

    // 记录删除日志
    for _, id := range delIds {
        log.Printf("用户删除成功: ID=%v", id)
    }

    return nil
}
```

### 查询操作钩子

#### ListQuery - 列表查询条件
```go
// 设置列表查询的WHERE条件
func (i *Index) ListQuery(query *gorm.DB) (*gorm.DB, error) {
    // 只显示启用的用户
    query = query.Where("status = ?", 1)
    
    // 根据请求参数添加搜索条件
    gpc := i.GetSafeMapGPC("all")
    
    if keyword, exists := gpc["keyword"]; exists && keyword != "" {
        query = query.Where("username LIKE ? OR email LIKE ?", 
            "%"+keyword.(string)+"%", "%"+keyword.(string)+"%")
    }
    
    if dept, exists := gpc["department"]; exists && dept != "" {
        query = query.Where("department = ?", dept)
    }

    return query, nil
}
```

#### ListEach - 列表数据处理
```go
// 对列表中的每个数据项进行处理
func (i *Index) ListEach(item interface{}) interface{} {
    user := item.(*userModel.Account)
    
    // 隐藏敏感信息
    user.Password = ""
    
    // 格式化时间显示
    user.CreatedAtFormat = user.CreatedAt.Format("2006-01-02 15:04:05")
    
    // 添加计算字段
    user.IsAdmin = user.Role == "admin"
    
    return user
}
```

#### ListSelect - 列表查询字段
```go
// 设置列表查询的SELECT字段
func (i *Index) ListSelect(query *gorm.DB) *gorm.DB {
    // 只选择需要的字段，提高查询性能
    return query.Select("id, username, email, status, created_at, updated_at")
}
```

#### ListOrder - 列表排序
```go
// 设置列表查询的排序规则
func (i *Index) ListOrder() interface{} {
    // 默认按创建时间倒序
    return "created_at DESC, id DESC"
}
```

### 保存操作钩子 ⭐ 兼容版

#### SaveBefore - 保存前钩子
```go
// 保存前的钩子方法（兼容创建和更新）
func (i *Index) SaveBefore(modelValue interface{}, mapData map[string]any, originalData ...interface{}) (interface{}, map[string]any, error) {
    user := modelValue.(*userModel.Account)
    
    if len(originalData) > 0 && originalData[0] != nil {
        // 更新操作 - 可以访问原始数据
        original := originalData[0].(*userModel.Account)
        
        // 基于原始数据进行处理
        if user.Username != original.Username {
            // 检查用户名重复
            var count int64
            i.DBI.GetDb().Model(&userModel.Account{}).
                Where("username = ? AND id != ?", user.Username, original.ID).
                Count(&count)
            if count > 0 {
                return nil, nil, errors.New("用户名已存在")
            }
        }
        
        // 密码处理
        if user.Password != "" && user.Password != original.Password {
            hashedPassword, _ := bcrypt.GenerateFromPassword([]byte(user.Password), bcrypt.DefaultCost)
            user.Password = string(hashedPassword)
        } else {
            user.Password = original.Password
        }
    } else {
        // 创建操作 - 没有原始数据
        
        // 检查用户名重复
        var count int64
        i.DBI.GetDb().Model(&userModel.Account{}).Where("username = ?", user.Username).Count(&count)
        if count > 0 {
            return nil, nil, errors.New("用户名已存在")
        }
        
        // 密码加密
        if user.Password != "" {
            hashedPassword, _ := bcrypt.GenerateFromPassword([]byte(user.Password), bcrypt.DefaultCost)
            user.Password = string(hashedPassword)
        }
        
        // 设置默认值
        if user.Status == 0 {
            user.Status = 1
        }
    }

    return user, mapData, nil
}
```

## 高级功能和最佳实践

### 自定义结果结构体
```go
// 自定义列表返回结构体
type UserListResult struct {
    ID       uint   `gorm:"column:id"`
    Username string `gorm:"column:username"`
    Email    string `gorm:"column:email"`
    Status   int    `gorm:"column:status"`
    IsOnline bool   `gorm:"column:is_online"` // 计算字段
}

func New() *Index {
    index := &Index{
        Trait: &crud.Trait{
            Model:            &userModel.Account{},
            DBI:              library.Mysql,
            ListResultStruct: &UserListResult{}, // 指定列表结果结构体
        },
    }
    
    index.Trait.Controller = index
    return index
}
```

### 表别名使用
```go
func New() *Index {
    index := &Index{
        Trait: &crud.Trait{
            Model:           &userModel.Account{},
            DBI:             library.Mysql,
            ModelTableAlias: "u", // 设置表别名
        },
    }
    
    index.Trait.Controller = index
    return index
}

// 在查询中使用别名
func (i *Index) ListQuery(query *gorm.DB) (*gorm.DB, error) {
    // 可以使用 u.字段名 的形式
    return query.Where("u.status = ?", 1), nil
}
```

### 复杂关联查询
```go
// 预加载关联数据
func (i *Index) ListQuery(query *gorm.DB) (*gorm.DB, error) {
    return query.Preload("Profile").Preload("Department"), nil
}

// 连接查询
func (i *Index) ListQuery(query *gorm.DB) (*gorm.DB, error) {
    return query.Joins("LEFT JOIN profiles p ON users.id = p.user_id").
        Select("users.*, p.avatar"), nil
}
```

### 权限控制集成
```go
// 基于用户权限的查询过滤
func (i *Index) ListQuery(query *gorm.DB) (*gorm.DB, error) {
    // 获取当前用户信息（假设从上下文中获取）
    currentUser := i.GetCurrentUser()
    
    if currentUser.Role != "admin" {
        // 非管理员只能看到自己部门的用户
        query = query.Where("department_id = ?", currentUser.DepartmentID)
    }
    
    return query, nil
}

// 删除权限检查
func (i *Index) DeleteBefore(delArr []map[string]interface{}, delIds []interface{}) ([]interface{}, error) {
    currentUser := i.GetCurrentUser()
    
    // 检查是否有权限删除这些用户
    for _, item := range delArr {
        userDeptID := item["department_id"]
        if currentUser.Role != "admin" && userDeptID != currentUser.DepartmentID {
            return nil, errors.New("没有权限删除其他部门的用户")
        }
    }
    
    return delIds, nil
}
```

### 性能优化技巧

#### 批量操作优化
```go
// 批量处理优化
func (i *Index) AllQuery(query *gorm.DB) *gorm.DB {
    // 使用批量查询，避免一次性加载大量数据
    return query.FindInBatches(&results, 1000, func(tx *gorm.DB, batch int) error {
        // 批量处理逻辑
        return nil
    })
}
```

#### 查询性能优化
```go
// 索引提示和查询优化
func (i *Index) ListSelect(query *gorm.DB) *gorm.DB {
    // 只选择必要的字段
    return query.Select("id, username, email, status, created_at")
}

func (i *Index) ListQuery(query *gorm.DB) (*gorm.DB, error) {
    // 使用索引字段进行查询
    if keyword := i.GetGPCString("keyword"); keyword != "" {
        // 使用 LIKE 查询时，确保字段有相应的索引
        query = query.Where("username LIKE ?", keyword+"%") // 前缀匹配性能更好
    }
    
    return query, nil
}
```

### 事务和锁定处理

#### 避免长时间锁定
```go
// 在 After 钩子中避免长时间操作
func (i *Index) CreateAfter(tx *gorm.DB, modelValue interface{}) error {
    user := modelValue.(*userModel.Account)
    
    // 轻量级的数据库操作
    if err := tx.Create(&userModel.Profile{UserID: user.ID}).Error; err != nil {
        return err
    }
    
    // 将耗时操作放到事务外异步执行
    go func() {
        // 发送欢迎邮件
        i.sendWelcomeEmail(user.Email)
        
        // 更新统计信息
        i.updateUserStatistics()
    }()
    
    return nil
}
```

#### 并发控制
```go
// 使用乐观锁防止并发更新冲突
func (i *Index) UpdateBefore(modelValue interface{}, mapData map[string]any, originalData interface{}) (interface{}, map[string]any, error) {
    user := modelValue.(*userModel.Account)
    original := originalData.(*userModel.Account)
    
    // 检查版本号（如果模型中有版本字段）
    if user.Version != original.Version {
        return nil, nil, errors.New("数据已被其他用户修改，请刷新后重试")
    }
    
    // 递增版本号
    user.Version = original.Version + 1
    
    return user, mapData, nil
}
```

### 错误处理和日志

#### 统一错误处理
```go
func (i *Index) CreateBefore(modelValue interface{}, mapData map[string]any) (interface{}, map[string]any, error) {
    defer func() {
        if r := recover(); r != nil {
            log.Printf("CreateBefore panic: %v", r)
            // 可以选择是否继续抛出 panic
        }
    }()
    
    // 业务逻辑...
    
    return modelValue, mapData, nil
}
```

#### 详细日志记录
```go
func (i *Index) UpdateAfter(tx *gorm.DB, modelValue interface{}) error {
    user := modelValue.(*userModel.Account)
    
    // 记录操作日志
    operationLog := &logModel.OperationLog{
        UserID:    i.GetCurrentUserID(),
        Action:    "UPDATE_USER",
        TargetID:  user.ID,
        Details:   fmt.Sprintf("更新用户: %s", user.Username),
        CreatedAt: time.Now(),
    }
    
    if err := tx.Create(operationLog).Error; err != nil {
        log.Printf("记录操作日志失败: %v", err)
        // 日志记录失败不应影响主要业务
    }
    
    return nil
}
```

## 调试和故障排除

### 开启调试模式
```go
// 在钩子方法中开启数据库调试
func (i *Index) ListQuery(query *gorm.DB) (*gorm.DB, error) {
    if os.Getenv("DEBUG") == "true" {
        query = query.Debug() // 打印SQL语句
    }
    
    return query.Where("status = ?", 1), nil
}
```

### 常见问题解决

#### 模型解析失败
```go
// 确保模型实现了 ModelParse 方法
func (u User) ModelParse(modelType reflect.Type) (tableName string, fields []string, softDeleteField string, softDeleteCondition string) {
    // 实现细节...
    return
}
```

#### 软删除不生效
```go
// 检查软删除字段配置
type User struct {
    base.MysqlBaseModel // 确保使用了基础模型
    // 或者手动配置软删除字段
    DeletedAt *time.Time `gorm:"index" json:"deleted_at"`
}
```

#### 事务回滚问题
```go
// 确保在 After 钩子中正确处理错误
func (i *Index) CreateAfter(tx *gorm.DB, modelValue interface{}) error {
    // 任何错误都会导致事务回滚
    if err := tx.Create(&profile).Error; err != nil {
        return err // 正确返回错误
    }
    
    return nil // 返回 nil 表示成功
}
```

## 升级和迁移指导

### 从旧版本升级
1. 更新钩子方法签名（特别是 UpdateBefore 和 SaveBefore）
2. 检查软删除字段配置
3. 更新错误处理逻辑
4. 测试事务行为

### 新功能采用
1. 使用新的钩子方法（如 SaveBefore 的可变参数）
2. 采用批量操作优化
3. 使用新的查询钩子方法
4. 集成性能监控
description:
globs:
alwaysApply: false
---
