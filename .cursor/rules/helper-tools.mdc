# Helper 工具包使用指南

## 组件概述
Helper 工具包提供了大量常用的工具函数，包括类型转换、文件操作、字符串处理、JSON 操作、SSH 密钥管理、金额处理等功能。

## 核心文件
- [component/helper/convert.go](mdc:component/helper/convert.go) - 类型转换工具
- [component/helper/file.go](mdc:component/helper/file.go) - 文件操作工具
- [component/helper/string.go](mdc:component/helper/string.go) - 字符串处理工具
- [component/helper/json.go](mdc:component/helper/json.go) - JSON 处理工具
- [component/helper/money.go](mdc:component/helper/money.go) - 金额处理工具
- [component/helper/ssh.go](mdc:component/helper/ssh.go) - SSH 密钥管理工具
- [component/helper/util.go](mdc:component/helper/util.go) - 通用工具函数
- [component/helper/ip.go](mdc:component/helper/ip.go) - IP 地址处理工具
- [component/helper/http.go](mdc:component/helper/http.go) - HTTP 请求处理工具
- [component/helper/unit.go](mdc:component/helper/unit.go) - 单位转换工具

## 类型转换工具 (convert.go)

### 基本使用
```go
import "github.com/jcbowen/jcbaseGo/component/helper"

// 创建转换器实例
convert := helper.Convert{Value: "123"}

// 转换为不同类型
intVal := convert.ToInt()        // 123
floatVal := convert.ToFloat64()  // 123.0
boolVal := convert.ToBool()      // true（非空字符串为 true）
stringVal := convert.ToString()  // "123"
```

### 各类型转换示例
```go
// 字符串转整数
stringToInt := helper.Convert{Value: "456"}.ToInt()
fmt.Println(stringToInt) // 456

// 数字转字符串
intToString := helper.Convert{Value: 789}.ToString()
fmt.Println(intToString) // "789"

// 字符串转布尔值
stringToBool := helper.Convert{Value: "true"}.ToBool()
fmt.Println(stringToBool) // true

// 接口类型转换
var data interface{} = "123.45"
floatFromInterface := helper.Convert{Value: data}.ToFloat64()
fmt.Println(floatFromInterface) // 123.45

// 批量转换
values := []interface{}{"123", "456.78", "true", 999}
for _, val := range values {
    converter := helper.Convert{Value: val}
    fmt.Printf("原值: %v, 整数: %d, 浮点: %.2f, 字符串: %s\n",
        val, converter.ToInt(), converter.ToFloat64(), converter.ToString())
}
```

## 文件操作工具 (file.go)

### 基本文件操作
```go
// 创建文件操作实例
fileHelper := helper.NewFile(&helper.File{Path: "/path/to/file.txt"})

// 检查文件是否存在
if fileHelper.Exists() {
    fmt.Println("文件存在")
}

// 读取JSON文件内容到变量
var data interface{}
err := fileHelper.JsonToData(&data)
if err != nil {
    log.Printf("读取JSON文件失败: %v", err)
} else {
    fmt.Printf("文件内容: %v", data)
}

// 创建文件
data := []byte("Hello, World!")
err = fileHelper.CreateFile(data, true) // true 表示自动创建目录
if err != nil {
    log.Printf("创建文件失败: %v", err)
}
```

### 目录操作
```go
// 检查目录是否存在，如果不存在则创建
dirHelper := helper.NewFile(&helper.File{Path: "/path/to/directory/"})
exists, err := dirHelper.DirExists(true) // true 表示不存在时自动创建
if err != nil {
    log.Printf("目录操作失败: %v", err)
}

// 获取目录名
dirName := dirHelper.DirName()
fmt.Printf("目录名: %s", dirName)

// 获取文件名（不含路径）
fileName := dirHelper.Basename("")
fmt.Printf("文件名: %s", fileName)
```

### 高级文件操作
```go
// 复制文件
srcFile := helper.NewFile(&helper.File{Path: "source.txt"})
destFile := helper.NewFile(&helper.File{Path: "destination.txt"})

// 复制文件内容
err := srcFile.CopyFile(destFile.Path, true, true)
if err != nil {
    log.Printf("复制文件失败: %v", err)
}

// 删除文件
err = srcFile.Remove()
if err != nil {
    log.Printf("删除文件失败: %v", err)
}

// 文件大小获取
fileInfo := helper.NewFile(&helper.File{Path: "data.txt"})
if fileInfo.Exists() {
    // 通过系统调用获取文件信息
    stat, err := os.Stat(fileInfo.Path)
    if err == nil {
        fmt.Printf("文件大小: %d 字节", stat.Size())
    }
}
```

## 字符串处理工具 (string.go)

### 字符串截取和处理
```go
// 创建字符串处理实例
text := "Hello, 世界！这是一个测试"
str := helper.NewStr(text)

// 字符串截取（按字符数）
substr := str.ByteSubstr(0, 10)
fmt.Printf("截取结果: %s", substr) // "Hello, 世界！"

// 字符串截断（带后缀）
truncated := str.Truncate(8, "...")
fmt.Printf("截断结果: %s", truncated)

// 检查字符串开头/结尾
startsWith := str.StartsWith("Hello", true)
endsWith := str.EndsWith("测试", true)

// 分割字符串
parts := str.Explode("，", true, true)
fmt.Printf("分割结果: %v", parts)
```

### 字符串转换和验证
```go
// 驼峰转下划线
str := helper.NewStr("UserProfile")
snake := str.ConvertCamelToSnake()
fmt.Printf("snake_case: %s", snake) // "user_profile"

// Base64 URL 编码/解码
encoded := str.Base64UrlEncode()
decoded, err := helper.NewStr(encoded).Base64UrlDecode()

// 检查是否为数字
isNum := helper.NewStr("123.45").IsNumeric()
isInt := helper.NewStr("123").IsNumeric(true) // 严格模式

// HTML 转义
escaped := helper.NewStr("<script>alert('xss')</script>").EscapeHTML()
```

## JSON 处理工具 (json.go)

### JSON 序列化和反序列化
```go
// 结构体转 JSON
type User struct {
    ID   int    `json:"id"`
    Name string `json:"name"`
    Age  int    `json:"age"`
}

user := User{ID: 1, Name: "张三", Age: 25}

// 转换为 JSON 字符串
var jsonStr string
err := helper.Json(user).ToString(&jsonStr)
if err != nil {
    log.Printf("JSON 序列化失败: %v", err)
} else {
    fmt.Printf("JSON 字符串: %s", jsonStr)
}

// JSON 字符串转结构体
jsonData := `{"id":2,"name":"李四","age":30}`
var newUser User
err = helper.Json(jsonData).ToStruct(&newUser)
if err != nil {
    log.Printf("JSON 反序列化失败: %v", err)
} else {
    fmt.Printf("用户信息: %+v", newUser)
}
```

### 复杂 JSON 操作
```go
// Map 转 JSON
dataMap := map[string]interface{}{
    "users": []User{
        {ID: 1, Name: "张三", Age: 25},
        {ID: 2, Name: "李四", Age: 30},
    },
    "total": 2,
    "page":  1,
}

var mapJsonStr string
err := helper.Json(dataMap).ToString(&mapJsonStr)
if err == nil {
    fmt.Printf("Map JSON: %s", mapJsonStr)
}

// JSON 字符串转 Map
var resultMap map[string]interface{}
err = helper.Json(mapJsonStr).ToMap(&resultMap)
if err == nil {
    fmt.Printf("解析结果: %+v", resultMap)
}

// 从文件读取 JSON
jsonFile := helper.JsonFile("./config.json")
var config map[string]interface{}
jsonFile.ToStruct(&config)
```

## SSH 密钥管理 (ssh.go)

### 获取和生成 SSH 密钥
```go
// 获取 SSH 公钥（如果不存在则自动生成）
publicKey, err := helper.GetSSHKey()
if err != nil {
    log.Printf("获取SSH公钥失败: %v", err)
} else {
    fmt.Printf("SSH公钥: %s", publicKey)
}

// 生成 SSH 密钥对
err = helper.GenerateSSHKey()
if err != nil {
    log.Printf("生成SSH密钥对失败: %v", err)
} else {
    fmt.Println("SSH密钥对生成成功")
}
```

## 金额处理工具 (money.go)

### 金额计算和格式化
```go
// 创建金额实例（以厘为单位，1000厘=1元）
money1 := helper.Money("12.34")
money2 := helper.Money("56.78")

// 金额加法
sum := helper.Money("0").SetAmount("12.34").Add(money2)
fmt.Printf("和: %s", sum.FloatString())

// 金额减法
difference := helper.Money("100").Subtract(helper.Money("50"))
fmt.Printf("差: %s", difference.FloatString())

// 金额乘法
product := helper.Money("10").Multiply(2.5)
fmt.Printf("积: %s", product.FloatString())

// 金额除法
quotient := helper.Money("100").Divide(4.0)
fmt.Printf("商: %s", quotient.FloatString())

// 金额格式化（带前缀和后缀）
formatted := helper.Money("12345.67").FloatString("¥", "元")
fmt.Printf("格式化金额: %s", formatted) // "¥12345.67元"

// 金额比较
m1 := helper.Money("100")
m2 := helper.Money("200")
fmt.Printf("大于: %t", m1.GreaterThan(m2)) // false
fmt.Printf("小于: %t", m1.LessThan(m2))    // true
fmt.Printf("等于: %t", m1.Equals(m1))      // true

// 检查错误
if err := m1.GetError(); err != nil {
    log.Printf("金额计算错误: %v", err)
}
```

## 通用工具函数 (util.go)

### 结构体操作
```go
// 检查并设置结构体默认值
type Config struct {
    Host    string `default:"localhost"`
    Port    int    `default:"8080"`
    Timeout int    `default:"30"`
}

config := &Config{}
err := helper.CheckAndSetDefault(config)
if err != nil {
    log.Printf("设置默认值失败: %v", err)
} else {
    fmt.Printf("配置: %+v", config)
    // 输出: 配置: &{Host:localhost Port:8080 Timeout:30}
}

// Map 转结构体
dataMap := map[string]interface{}{
    "host":    "example.com",
    "port":    3306,
    "timeout": 60,
}

var dbConfig Config
helper.MapToStruct(dataMap, &dbConfig)
fmt.Printf("数据库配置: %+v", dbConfig)
```

### 其他实用工具
```go
// 获取主机信息（需要 HTTP 请求对象）
hostInfo := helper.GetHostInfo(request)
fmt.Printf("主机信息: %s", hostInfo)

// MD5 哈希
text := "Hello, World!"
md5Hash := helper.MD5(text)
fmt.Printf("MD5 哈希: %s", md5Hash)

// SHA256 哈希
sha256Hash := helper.SHA256(text)
fmt.Printf("SHA256 哈希: %s", sha256Hash)

// 生成随机字符串
randomStr := helper.Random(16)
fmt.Printf("随机字符串: %s", randomStr)

// IP 地址处理
ip := helper.NewIP("192.168.1.1")
if ip.IsValid() {
    fmt.Printf("IP 有效，是否为私有地址: %t", ip.IsPrivate())
}

// HTTP 头提取
headers := helper.ExtractHeaders(request.Header)
fmt.Printf("请求头: %+v", headers)

// 单位转换
storageSize := helper.ParseUnit("1.5GB")
fmt.Printf("存储大小: %.0f 字节", storageSize)

// 时间单位转换
timeValue := helper.ParseUnit("2h")
fmt.Printf("时间: %.0f 秒", timeValue)
```

## 最佳实践

### 错误处理
```go
// 始终检查错误返回值
fileHelper := helper.NewFile(&helper.File{Path: "config.json"})
var data interface{}
err := fileHelper.JsonToData(&data)
if err != nil {
    log.Printf("读取配置文件失败: %v", err)
    // 使用默认配置或退出程序
    return
}

// 处理文件内容...
```

### 性能优化
```go
// 对于频繁的类型转换，考虑缓存转换器
converter := helper.Convert{Value: someValue}
intVal := converter.ToInt()
strVal := converter.ToString() // 复用转换器实例

// 批量文件操作时，考虑并发处理
files := []string{"file1.txt", "file2.txt", "file3.txt"}
var wg sync.WaitGroup

for _, filename := range files {
    wg.Add(1)
    go func(file string) {
        defer wg.Done()

        fileHelper := helper.NewFile(&helper.File{Path: file})
        if fileHelper.Exists() {
            var data interface{}
            _ = fileHelper.JsonToData(&data)
            // 处理文件内容
            fmt.Printf("处理文件: %s\n", file)
        }
    }(filename)
}

wg.Wait()
```

### 安全注意事项
- SSH 连接使用密钥认证而非密码认证
- 文件操作前验证路径，防止路径遍历攻击
- JSON 处理时注意数据大小限制
- 金额计算使用专门的金额函数避免精度问题

## 扩展和自定义

### 自定义类型转换
```go
// 扩展转换器支持自定义类型
type CustomConverter struct {
    helper.Convert
}

func (c *CustomConverter) ToCustomType() CustomType {
    // 实现自定义类型转换逻辑
    return CustomType{Value: c.ToString()}
}
```

### 自定义文件操作
```go
// 扩展文件操作功能
type EnhancedFile struct {
    *helper.File
}

func (ef *EnhancedFile) ReadJSON(v interface{}) error {
    return helper.JsonFile(ef.Path).ToStruct(v)
}

func (ef *EnhancedFile) WriteJSON(v interface{}) error {
    var jsonStr string
    err := helper.Json(v).ToString(&jsonStr)
    if err != nil {
        return err
    }

    return ef.CreateFile([]byte(jsonStr), true)
}
```
description:
globs:
alwaysApply: false
---
